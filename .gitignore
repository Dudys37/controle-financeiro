# ── Secrets / ambiente — NUNCA versionar ──
.env
.env.*
.dev.vars
worker/.dev.vars
worker/wrangler.toml
*.local
deploy.local.ps1
secrets.*
*.secret
firebase-debug.log
firebase-debug.*.log
.firebase/

# ── Node / Worker ──
node_modules/
worker/node_modules/
dist/
worker/dist/

# ── Certificados / chaves ──
*.pem
*.key
*.crt
*.p12
*.pfx

# ── Autenticação legada (descontinuada) ──
auth.js

# ── SO / editores ──
.DS_Store
Thumbs.db
.vscode/
.idea/
